.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust build outputs
src-tauri/target/

# Tauri signing keys — must NEVER be committed; they belong in your home dir
# and as GitHub Actions secrets, not in the repo.
*.key
*.key.pub
